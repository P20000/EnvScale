PORT=8080
NODE_ENV=development
JWT_ACCESS_SECRET=replace-with-a-long-random-access-secret
REDIS_URL=redis://localhost:6379

# Set ALLOW_ANONYMOUS_WS=true for local dev without mandatory UI login
# Set ALLOW_ANONYMOUS_WS=false to enforce strict JWT authentication on all WebSocket connections
ALLOW_ANONYMOUS_WS=true
